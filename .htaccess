RewriteEngine On

# Get rid of .html extensions 
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# SSL / HTTPS Support
RewriteCond %{SERVER_PORT} 80
RewriteRule ^(.*)$ https://sandhelper.com/$1 [R=301,L]